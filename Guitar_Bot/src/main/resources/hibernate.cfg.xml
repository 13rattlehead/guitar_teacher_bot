<hibernate-configuration>
    <session-factory>
        <!-- JDBC -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>
        <property name="hibernate.connection.url">jdbc:postgresql://localhost:5432/gb</property>
        <property name="hibernate.connection.username">postgres</property>
        <property name="hibernate.connection.password">1122</property>

        <!-- Dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Автогенерация таблиц -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Лог SQL -->
        <property name="hibernate.show_sql">true</property>

        <!-- Сущности -->
        <mapping class="denis_ryindin.model.Student"/>
        <mapping class="denis_ryindin.model.Lesson"/>
        <mapping class="denis_ryindin.model.Homework"/>
    </session-factory>
</hibernate-configuration>
